int g1, g2, *g3;
int *g4;
int array[100], array2[2][3][4], *p, *arraypunt[20];

int main() {
  int *PUNT;
  int a, b;
  int ar[50];

  g1 = g1 + (g2 + g1);

  scanf("%d", &b);

  if (a == b) {
    g2 = a + 1;
  } else {
    g2 = b + 2;
  }

  return 0;
}

void x() {
  int b, c;
  printf("--> %d %d", b, c);
}
void y(int a) {}
