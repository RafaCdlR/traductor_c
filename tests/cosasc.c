int g1, g2, *g3;
int *g4;
int array[100], array2[2][3][4], *p, *arraypunt[20];

int main(int *a, int b) {
  int *PUNT;
  int ar[50];

  g1 = g1 + (g2 + g1);

  scanf("%d", &b);

  if (a == b) {
    a + 1;
  } else {
    b + 2;
  }

  return 1;
}

void x() {
  int b, c;
  printf("--> %d %d", b, c, d);
}
void y(int a) {}
